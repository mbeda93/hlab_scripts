# hlab_scripts
Assortment of scripts to automate my hosted apps.

# ipc_auth

## Depends on:
* A functioning instance of tuya-ipc-terminal, install as a systemd service
* pexpect (can be installed using <pip3 install pexpect>)